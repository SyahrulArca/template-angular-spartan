/** Endpoint login — backend set cookie via `Set-Cookie`. */
export const AUTH_LOGIN_PATH = '/auth/login';

/** Endpoint logout — backend hapus cookie auth. */
export const AUTH_LOGOUT_PATH = '/auth/logout';

/** Endpoint refresh session — backend set cookie baru. */
export const AUTH_REFRESH_PATH = '/auth/refresh';

/** Endpoint validasi session saat app boot / guard. */
export const AUTH_ME_PATH = '/auth/me';
